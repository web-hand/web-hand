name: Check PR

on:
  pull_request:
    branches: [master]

jobs:
  lint-and-test:
    uses: ./.github/workflows/lint-and-test.yml

  check-pr:
    name: Check PR
    runs-on: ubuntu-latest
    steps:
      - name: Build 🧬
        run: npm run build
